# Fonts Directory

Place your Vietnamese font file here (e.g., `Roboto-Regular.ttf`).

The PDF export feature requires a font file that supports Vietnamese characters (Unicode).

**Required font file:**
- `Roboto-Regular.ttf` - A font file that supports Vietnamese characters

You can download Roboto font from Google Fonts or use any other font that supports Vietnamese Unicode characters.

